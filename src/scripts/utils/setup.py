from setuptools import setup, find_packages

setup(
    name='utils',  # Your library's name
    version='0.1.0',  # Version number
    packages=find_packages(),  # Automatically find packages (important!)
    install_requires=[],  # Dependencies (if any)
    # Add other metadata if needed (author, email, description, etc.)
)